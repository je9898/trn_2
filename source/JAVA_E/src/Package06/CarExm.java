package Package06;

public class CarExm {
/*    public static void main(String[] args) {
        Car myCar = new Car();  // 인스턴스 생성 Car라는 클래스 전체를 오브젝트로 봐서 Car라는 class를 갖다 사용하겠따

        myCar.setSpeed(-100);
        System.out.println(myCar.getSpeed());   // 위에서 인스턴스 생성해줘서 getSpeed를 끌어다 쓸 수 있는것임!

        myCar.setSpeed(60);
        if (myCar.isStop()) {
           // myCar.setSpeed() = true;
            System.out.println("저세상까지 "+ myCar.getSpeed() +"km/s로 주행 중ㅇ_<");
        }
    }*/
}
